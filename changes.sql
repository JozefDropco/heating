alter table `TEMP_MEASURE_PLACE` add ORDER_ID int null;

UPDATE `TEMP_MEASURE_PLACE` t
SET t.ORDER_ID = 2
WHERE t.PLACE_REF_CD LIKE 'TA3' ESCAPE '#';

UPDATE `TEMP_MEASURE_PLACE` t
SET t.ORDER_ID = 3
WHERE t.PLACE_REF_CD LIKE 'PRED#_TA3' ESCAPE '#';

UPDATE `TEMP_MEASURE_PLACE` t
SET t.ORDER_ID = 1
WHERE t.PLACE_REF_CD LIKE 'SOLAR' ESCAPE '#';

UPDATE `TEMP_MEASURE_PLACE` t
SET t.ORDER_ID = 4
WHERE t.PLACE_REF_CD LIKE 'EXT#_TEMP' ESCAPE '#';

